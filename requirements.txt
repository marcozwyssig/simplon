# Runtime deps of the delivery kernel (the domain-agnostic *ctl shared core). A consuming product's
# host-venv requirements.txt references this file with `-r` and adds ONLY its own product deps, so the
# kernel owns its dependency declaration rather than every product re-pinning it (netctl#730).
#
# These are exactly what `delivery` imports: typer (the manifest-assembled CLI), pydantic (the manifest
# schema), PyYAML (context + manifest parsing), textual (the split-pane step TUI), and - since netctl#731
# Strand 2 - jinja2 + cryptography (the generic clab render engine, delivery.clabrender: template rendering
# + WireGuard X25519 keygen). click and rich are transitive but pinned here because the pins are about the
# kernel's own CLI/TUI, not the product.
typer==0.12.5
click==8.1.7        # typer 0.12.5 predates Click 8.2's make_metavar(ctx) change; pin the last 8.1.x
PyYAML==6.0.3
rich==15.0.0        # pulled by textual 8.2.8; the kernel uses plain ANSI, not rich, so the bump is safe
textual==8.2.8      # the split-pane step TUI (doctor + build/up/seed); headless fallback without it
pydantic==2.13.4    # the delivery manifest schema (delivery.orchestrator.manifest.load, netctl#651 strand 3)
jinja2==3.1.4       # the delivery.clabrender template engine (netctl#731 Strand 2)
cryptography==46.0.0  # delivery.clabrender.wg_keypair - WireGuard X25519 keygen (netctl#731 Strand 2).
                      # 46.0.0 is the FIRST release publishing win_arm64 wheels. Below it pip falls back
                      # to building from source on Windows/ARM, which needs a Rust toolchain and fails on
                      # a stock runner - so a product targeting that platform cannot install the kernel at
                      # all. Only X25519PrivateKey.generate()/private_bytes_raw() is used and that API is
                      # unchanged across 43 -> 46.
mypy==1.18.2          # the test:typecheck-python gate (delivery.tasks.typecheck). In the KERNEL's
                      # set, not each product's: the task is the kernel's, so the tool it runs is
                      # too, and a product adopts the gate with one manifest line plus its own
                      # mypy.ini. types-PyYAML because PyYAML is pinned right here - a stub for a
                      # kernel dependency belongs beside the dependency.
types-PyYAML==6.0.12.20250915
