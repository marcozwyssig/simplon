"""simplon - host-Python delivery orchestrator, scaffolded on the delivery kernel (netctl#651 strand 4).

The CLI is assembled from simplon.yaml by the delivery binding layer; see cli.py for the composition
root and paths.py for the ProductContext wiring. Grow the CLI by editing the manifest, not this package.
"""

__version__ = "0.1.0"
